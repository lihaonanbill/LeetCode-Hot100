1. 35.searchInsert
    - 数组，二分查找
2. 74.searchMatrix
    - 数组，二分查找，矩阵
    - 注意这题和35的左右查找边界的区别，35中还需要考虑不存在插入的情况
    - 这两题有点绕
3. 34.searchRange
    - 数组，二分查找
4. 33.search
    - 数组，二分查找
5. 153.findMin
    - 数组，二分查找
6. 4.findMedianSortedArrays
    - 数组，二分查找，分治



# 二分查找区间写法详解：左闭右开 vs 左闭右闭

## 📊 两种区间的核心对比

| 特性 | **左闭右开 `[left, right)`** | **左闭右闭 `[left, right]`** |
| :--- | :--- | :--- |
| **初始化** | `left = 0, right = len(nums)` | `left = 0, right = len(nums) - 1` |
| **区间含义** | 搜索范围包含 `left`，**不包含** `right` | 搜索范围包含 `left`，也包含 `right` |
| **循环条件** | **`while left < right:`**<br>当 `left == right` 时，区间 `[left, left)` 为空 | **`while left <= right:`**<br>当 `left == right` 时，区间 `[left, left]` 仍有一个元素 |
| **更新 `left`** | `left = mid + 1` | `left = mid + 1` |
| **更新 `right`** | **`right = mid`**<br>因为 `mid` 已检查过，新区间不包含它 | **`right = mid - 1`**<br>因为 `mid` 已检查过，新区间需排除它 |
| **最终情况** | 循环结束时 `left == right`，该值表示**第一个不满足左边条件的索引**（如 `>=target` 的位置） | 循环结束时 `left > right`，目标若存在，其索引是 `mid` |
| **推荐场景** | **优先推荐**，尤其适合查找边界、插入位置等问题 | 符合直觉，适合简单"是否存在"的查找 |

## 🧪 两种写法的标准模板

### 模板一：左闭右开 `[left, right)`
```python
def binary_search(nums, target):
    left, right = 0, len(nums)  # 注意right初始值
    while left < right:         # 关键：因为right不可达，所以 <
        mid = (left + right) // 2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1      # 目标在右半部分，移动左边界
        else: # nums[mid] > target
            right = mid         # 关键：目标在左半部分，移动右边界 (mid不可达)
    return -1                   # 未找到
```

### 模板二：左闭右闭 `[left, right]`
```python
def binary_search(nums, target):
    left, right = 0, len(nums) - 1  # 注意right初始值
    while left <= right:            # 关键：因为right可达，所以 <=
        mid = (left + right) // 2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1          # 目标在右半部分，移动左边界
        else: # nums[mid] > target
            right = mid - 1         # 关键：目标在左半部分，移动右边界 (mid已排除)
    return -1                       # 未找到
```

代码中三处不同
    1.right初始值
    2.循环停止条件
    3.右边界移动位置